#include<stdio.>
int fibonacci(n)
{
if(n==1)
{return 0;}
else{
    if
}
}
int main()
{int n,m;
printf("enter the term to be found ");
scanf("%d",&n);
m=fibonacci(n);
printf("the required term is %d ",m);

return 0;

}